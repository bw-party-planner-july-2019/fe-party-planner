const initialState = {
    user: {},
    isAuth: false,
    isLoading: false,
    errors: null
};

export const authReducer = (state = initialState, {type, payload}) => {
    switch (type) {
        default:
            return state;
    }
};
