const initialState = {
	party     : [],
	isLoading : false,
	errors    : null,
};

export const eventsReducer = (state = initialState, { type, payload }) => {
	switch (type) {
		default:
			return state;
	}
};
